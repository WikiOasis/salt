{#- Managed by Salt (incidentio_sync). Do not edit manually.
   Non-secret values come from pillar/incidentio_sync; the webhook URL is a
   secret and comes from pillar/private (gitignored) — see
   pillar/private/init.sls.example for the shape. #}
{%- set p = salt['pillar.get']('incidentio_sync', {}) %}
DISCORD_WEBHOOK_URL={{ salt['pillar.get']('incidentio_sync:discord_webhook_url', '') }}
FEED_URL={{ p.get('feed_url', 'https://status.wikioasis.org/feed.atom') }}
STATE_FILE={{ p.get('state_file', '/var/lib/incidentio-sync/state.json') }}
STATUS_PAGE_URL={{ p.get('status_page_url', 'status.wikioasis.org') }}
POLL_INTERVAL={{ p.get('poll_interval', 15) }}
PRUNE_AFTER_DAYS={{ p.get('prune_after_days', 30) }}
LOG_LEVEL={{ p.get('log_level', 'INFO') }}
